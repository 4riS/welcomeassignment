<nav class="navbar navbar-inverse">
  <div class="container">
    <div class="navbar-header">
      <%= link_to 'Blog', root_path, class: 'navbar-brand' %>
    </div>
    <div id="navbar">
  
    <ul class="nav navbar-nav pull-right">
      <li><%= link_to 'Home', root_path %></li>
      <li><%= link_to 'Users', users_path %></li>
      <li><%= link_to 'Hashtags', hashtags_path %></li>
    <% if current_user %>
    <li><span class="signin_as"><%= current_user.name %></span></li>
      <%= image_tag current_user.photo_url, :class => "img-circle" %>
    <li><%= link_to 'Log Out', signout_path, method: :delete %></li>
  <% else %>
    <li><%= link_to "Sign in with Facebook", "/auth/facebook" %></li>
  <% end %>   
  </ul> 
  </div>
  </div>
</nav>